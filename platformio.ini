[env:esp32dev]
platform = espressif32
board = esp32dev

extra_scripts = pre:patch.py
board_build.ldscript = linker/esp32.ld

build_flags =
    -nostdlib       
    -nostartfiles   
    -fno-builtin

monitor_speed = 115200
board_build.f_cpu = 240000000L
